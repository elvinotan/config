---
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9083/eureka
server:
  port: 9087
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh,refresh